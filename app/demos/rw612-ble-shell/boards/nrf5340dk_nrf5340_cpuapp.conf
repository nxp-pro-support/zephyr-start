# Board-specific overrides were trimmed to avoid deprecated or removed
# symbols in newer Zephyr revisions. The demo still builds on nRF5340DK
# using the common Bluetooth shell configuration from prj.conf.
